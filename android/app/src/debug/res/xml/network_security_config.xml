<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may target the emulator host's ephemeral local CI backend. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
