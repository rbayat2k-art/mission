# انتشار 2.6.0 — اعلان گوشی و مدیریت حساب برای همه نقش‌ها

- زنگ اعلان کارمند، سرپرست و مدیر به مرکز اعلان واقعی متصل شد و تعداد درخواست‌های باز هر نقش را نشان می‌دهد.
- تخصیص مأموریت جدید و نتیجه تأیید/رد/اصلاح به‌صورت اعلان داخل سامانه ذخیره می‌شود.
- ارسال Web Push استاندارد با Service Worker و کلیدهای VAPID برای دریافت اعلان روی گوشی اضافه شد.
- تنظیم اعلان برای همه کاربران به‌طور پیش‌فرض فعال است و هر کاربر می‌تواند اعلان گوشی را خاموش یا دوباره فعال کند.
- صفحه «حساب و امنیت» برای کارمند، سرپرست، مدیر و مالک اضافه شد؛ تغییر نام، نام کاربری و رمز با تأیید رمز فعلی انجام می‌شود.
- تغییر اطلاعات حساس، نشست‌های قبلی را می‌بندد، نشست امن تازه می‌سازد و در Audit Log ثبت می‌کند.
- Manifest نصب وب‌اپ اضافه شد تا استفاده تمام‌صفحه روی Android و iPhone مناسب‌تر باشد.

## قابلیت‌های حفظ‌شده از 2.5.0 — پیگیری مجدد مأموریت و سیاست کامل کار روزانه

- فقط نتیجه «انجام شد» مأموریت را مختومه می‌کند؛ نتیجه‌های «نیاز به پیگیری»، «مسئول نبود»، «تعطیل بود»، «موکول شد» و «سایر» وارد دسته «پیگیری مجدد» می‌شوند.
- هر مراجعه در جدول مستقل `mission_attempts` نگه‌داری می‌شود تا نتیجه، گزارش، مقصد، زمان، نقاط GPS، هزینه و امتیاز تلاش قبلی از بین نرود.
- مأموریت خودساخته با نتیجه ناموفق در وضعیت «پیگیری مجدد · منتظر بررسی» می‌ماند و پس از تأیید سرپرست برای مراجعه بعدی فعال می‌شود.
- اپ کارمند دسته مستقل «پیگیری مجدد»، سابقه تعداد مراجعات و دکمه شروع مراجعه بعدی دارد.
- پنل مدیر، مالک و سرپرست دارای فیلترهای واقعی «همه، باز، در حال انجام، منتظر تأیید، پیگیری مجدد و انجام‌شده» است.
- کارکرد روزانه بر اساس GPS معتبر محاسبه می‌شود؛ حداقل روزانه ۸ ساعت و ۳۰ دقیقه و شروع اضافه‌کاری پس از ۹ ساعت است.
- توقف بین دو فعالیت در کارکرد حساب نمی‌شود، فعالیت بدون GPS معتبر محاسبه نمی‌شود و فعالیت عادی در ۹ ساعت به‌صورت سیستمی پایان می‌یابد.
- خوداظهاری شروع فراموش‌شده برای همان روز با ثبت دلیل، کسر ۳ امتیاز و تأیید سرپرست پشتیبانی می‌شود.

## قابلیت‌های حفظ‌شده از 2.4.0 — زمان و مسافت مسیر هر مأموریت

- تأخیر روزانه به‌صورت شفاف از اولین شروع فعالیت بعد از ساعت ۰۸:۳۰ محاسبه می‌شود.
- زمان در مسیر برای هر مأموریت از لحظه «شروع مأموریت» تا لحظه «ثبت مقصد» محاسبه می‌شود.
- زمان واقعی حرکت و زمان توقف داخل مسیر با استفاده از سرعت و توالی نقاط GPS از هم جدا می‌شوند.
- مسافت پیموده‌شده هر مأموریت جداگانه محاسبه و در گزارش کارمند، پنل مدیر و خروجی CSV/چاپ نمایش داده می‌شود.
- کیفیت پوشش GPS هر مسیر با وضعیت کامل، ناقص یا بدون داده مشخص می‌شود تا عدد تقریبی با داده کامل اشتباه نشود.
- زمان حضور در مقصد اکنون از ثبت مقصد تا پایان مأموریت محاسبه می‌شود و با زمان مسیر هم‌پوشانی ندارد.

## قابلیت‌های حفظ‌شده از 2.3.0 — ممیزی سه‌نقطه‌ای و نمره مدیر

- لحظه زدن «شروع کار روی مأموریت»، مختصات، دقت GPS و زمان دستگاه به‌صورت مستقیم ذخیره می‌شود.
- لحظه ثبت نتیجه و پایان مأموریت نیز مختصات و دقت GPS پایان ذخیره می‌شود.
- مدیر، مالک و سرپرست مجاز با زدن روی مأموریت تمام‌شده، نقطه شروع، مقصد و پایان را روی یک نقشه می‌بینند.
- فاصله شروع تا مقصد، فاصله مقصد تا پایان و مدت کل مأموریت برای ارزیابی محاسبه می‌شود.
- پایان دورتر از ۱۵۰ متر از مقصد، دقت پایین GPS، نقطه گمشده و شروع‌نشدن مأموریت به‌صورت هشدار ارزیابی نمایش داده می‌شود.
- برای سوابق قدیمی، نزدیک‌ترین GPS حداکثر دو دقیقه‌ای فقط با برچسب واضح «برآوردی» نمایش داده می‌شود و با ثبت مستقیم اشتباه گرفته نمی‌شود.
- مدیر یا سرپرست می‌تواند بر اساس این شواهد، نمره صفر تا ۱۲ و دلیل ارزیابی را ثبت کند.
- نمره مأموریت خودساخته تا تأیید سرپرست Pending می‌ماند؛ نمره مأموریت تأییدشده قطعی ذخیره می‌شود.
- تغییر نمره و دلیل آن در Audit Log با نام ارزیاب ثبت می‌شود.
- سرپرست فقط مأموریت کارکنان مستقیم زیرمجموعه خودش را می‌بیند و ارزیابی می‌کند.

## قابلیت‌های حفظ‌شده از 2.2.0

- «ثبت مقصد» کارمند اکنون مختصات واقعی GPS را به همان مأموریت متصل و در دیتابیس ذخیره می‌کند.
- مقصدهای هر کارمند در هر روز به‌ترتیب زمان با شماره‌های ۱، ۲، ۳ و ... مشخص می‌شوند.
- پنل مدیر مکان فعلی نیروها و همه مقصدهای شماره‌دار امروز را هم‌زمان روی نقشه تعاملی نشان می‌دهد.
- بین مقصدهای متوالی هر فرد در همان روز یک خط راهنما رسم می‌شود.
- در گزارش روزانه، هفتگی و ماهانه، نقشه و فهرست مقصدهای هر کارمند به‌صورت جداگانه نمایش داده می‌شود؛ شماره‌گذاری در هر روز از ۱ شروع می‌شود.
- دسترسی سرپرست به نقشه و مقصدها فقط به کارکنان زیرمجموعه خودش محدود است.
- اپ کارمند نقشه یا مسیر را نمایش نمی‌دهد و فقط نام مقصد و موقعیت فعلی را ثبت می‌کند.
- ثبت نتیجه مأموریت بدون وجود مقصد GPS در Backend پذیرفته نمی‌شود.
- ثبت مقصد آفلاین در صف امن گوشی نگه‌داری و پس از اتصال همگام می‌شود.

## قابلیت‌های حفظ‌شده از 2.1.0

- مأموریت‌های باز ابتدا صفحه شرح وظیفه را نمایش می‌دهند.
- مشاهده مأموریت باعث شروع خودکار یا قفل‌شدن آن نمی‌شود.
- کارمند از میان مأموریت‌های متعدد، کار موردنظر را انتخاب و «شروع کار» را ثبت می‌کند.
- گزینه «ثبت مقصد» مستقیماً فهرست مأموریت‌های باز را نمایش می‌دهد.
- پایان مأموریت بدون ثبت شروع مجاز است، اما ۳ امتیاز از امتیاز پایه ۱۲ کسر می‌شود.
- دلیل کسر در اعلان کارمند، نتیجه نهایی، دیتابیس، Audit Log و مرکز یکپارچگی ثبت می‌شود.
- گزارش عملکرد تعداد مأموریت‌های بدون شروع و مجموع امتیاز کسرشده را محاسبه می‌کند.
- Migration سازگار برای ستون‌های جدید امتیاز روی دیتابیس موجود اضافه شد.

این نسخه تمام تغییرات معماری مستقل سرور شامل Node.js، MySQL، فایل محلی، cPanel، PM2، Apache/Nginx و SSL را نیز دارد.
