# Backend سامانه راهکار

## معماری مستقل سرور

- Node.js 22 و Next.js 16
- MySQL/MariaDB با Pool اتصال
- ذخیره مدارک روی دیسک سرور در مسیر قابل تنظیم `UPLOAD_DIR`
- Session Cookie از نوع HttpOnly و SameSite=Strict
- تشخیص HTTPS از `X-Forwarded-Proto` برای اجرای امن پشت CDN و Reverse Proxy
- PBKDF2 با Salt مستقل و ۲۱۰٬۰۰۰ تکرار برای رمزها
- IndexedDB در مرورگر برای صف آفلاین GPS و رویدادها

هیچ Import یا Binding از Cloudflare Workers، D1 و R2 در Backend وجود ندارد.

## داده اولیه

دیتابیس خام است و فقط در اولین اجرای دیتابیس خالی، مدیر اولیه ساخته می‌شود:

- `admin`
- `123123456`

کاربر، مأموریت، موقعیت، گزارش، هشدار یا فایل نمایشی دیگری ساخته نمی‌شود.

## نقش‌ها

- Owner/Admin: مدیریت کامل کاربران، مأموریت‌ها، گزارش‌ها، موقعیت‌ها و تأییدها
- Supervisor: فقط کاربران مستقیم زیرمجموعه و مأموریت‌ها/گزارش‌های همان افراد
- Employee: فعالیت روزانه، مأموریت‌های خود، مدارک، هزینه و گزارش شخصی

فقط Employee سرپرست دارد. مأموریت خودساخته کارمند پس از تکمیل Pending می‌شود و امتیاز آن تا تأیید سرپرست قطعی نیست.

## APIهای اصلی

| مسیر | کاربرد |
|---|---|
| `/api/health` | سلامت برنامه و اتصال MySQL |
| `/api/auth/*` | ورود، خروج، نشست و تغییر رمز |
| `/api/account` | ویرایش امن نام، نام کاربری و رمز همه نقش‌ها |
| `/api/notifications/*` | مرکز اعلان، تنظیم کاربر و اشتراک Web Push دستگاه |
| `/api/admin/users/*` | ساخت، ویرایش، رمز، فعال/غیرفعال و حذف کاربر |
| `/api/work-sessions` | شروع و پایان فعالیت روزانه |
| `/api/missions/*` | ایجاد، تخصیص، شروع، نتیجه، ویرایش و حذف مأموریت |
| `/api/locations` | ثبت دسته‌ای GPS و مشاهده نقش‌محور |
| `/api/integrity` | هشدار قطع GPS، آفلاین و دقت پایین |
| `/api/attachments/*` | چند عکس یا رسید JPG/PNG/PDF تا ۱۰ مگابایت |
| `/api/approvals/*` | تأیید، رد یا برگشت برای اصلاح |
| `/api/reports/*` | گزارش روزانه، هفتگی، ماهانه، CSV و چاپ |

## دیتابیس

Schema قابل تکرار در `db/mysql-schema.sql` قرار دارد و با `npm run db:migrate` اجرا می‌شود. جدول‌ها:

`users`, `sessions`, `notifications`, `push_subscriptions`, `work_sessions`, `missions`, `approvals`, `audit_logs`, `location_points`, `integrity_events`, `attachments`

زمان‌های اصلی از ساعت سرور به ISO/UTC ثبت و در رابط با تقویم شمسی نمایش داده می‌شوند.

## فایل‌ها

فایل واقعی در `UPLOAD_DIR` و مشخصات آن در MySQL ذخیره می‌شود. کلید فایل اعتبارسنجی می‌شود و امکان خروج از پوشه Upload وجود ندارد. دانلود و حذف نیز کنترل نقش و مالکیت مأموریت دارند.

در Production از مسیر Upload و دیتابیس به‌صورت روزانه نسخه پشتیبان بگیرید.

## محدودیت حذف

- مأموریت پس از شروع قابل ویرایش یا حذف نیست.
- مدرک پس از پایان مأموریت قفل می‌شود.
- کاربر دارای سابقه عملیاتی حذف نمی‌شود و باید غیرفعال شود.
- تغییر رمز یا غیرفعال‌سازی، نشست‌های فعال کاربر را باطل می‌کند.

## قاعده شروع مأموریت و کسر امتیاز

مشاهده شرح مأموریت وضعیت آن را تغییر نمی‌دهد. دکمه «شروع کار» زمان `started_at` را با ساعت سرور ثبت و مأموریت را قفل می‌کند. کارمند با فعالیت روزانه باز می‌تواند بدون زدن شروع نیز مقصد و نتیجه را ثبت کند؛ در این حالت امتیاز پایه ۱۲ به ۹ کاهش می‌یابد. کسر ۳ امتیاز و علت آن در `score_penalty` و `score_note`، Audit Log و یک رویداد `mission_completed_without_start` ذخیره می‌شود.

راهنمای استقرار عملی: `deploy/CPANEL_DEPLOYMENT.md`.
